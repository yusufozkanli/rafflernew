console.log('Hello world')

const participant_link = document.getElementById("participant_tab");
participant_link.addEventListener("click", (event) => {
  console.log('Tab clicked')
  // Get the user's tickets where the raffle.user != the current user
});


